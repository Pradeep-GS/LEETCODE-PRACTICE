class Solution(object):
    def sumAndMultiply(self, n):
        lis = list(str(n))
        num = 0
        su = 0
        for j in lis:
            if j!='0':
                num = num*10+int(j)
                su+=int(j)
        return num*su
        
